import 'package:flutter/material.dart';

class cricketApi extends StatelessWidget {
  const cricketApi({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Live Score'),
      ),
    );
  }
}